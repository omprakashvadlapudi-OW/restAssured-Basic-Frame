# Credentials Configuration Template
# INSTRUCTIONS:
# 1. Copy this file and rename to: credentials.properties
# 2. Replace the default values with your real credentials
# 3. NEVER commit credentials.properties to Git

# Login credentials
# Format: ${ENV_VAR:default_value}
# Priority: Environment Variable > This file > Default value

# Replace these with your real credentials:
login.username=${LOGIN_USERNAME:twl4admin@gmail.com}
login.password=${LOGIN_PASSWORD:twl4test}

# For different environments, you can create:
# - credentials-dev.properties
# - credentials-qa.properties  
# - credentials-prod.properties

# In CI/CD, set environment variables:
# - LOGIN_USERNAME
# - LOGIN_PASSWORD
